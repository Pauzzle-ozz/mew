# Frontend — Mew

Interface Next.js 16 (App Router) de la plateforme Mew.

Voir le [README principal](../README.md) pour l'installation complète (variables d'environnement, Supabase, backend).

```bash
npm install
npm run dev    # http://localhost:3000 (le backend doit tourner sur le port 5000)
```
